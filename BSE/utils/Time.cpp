#include <utils/Time.h>

namespace BSE {
	//Time::m_Last = std::chrono::system_clock::now();
	//Time::m_Now = std::chrono::system_clock::now();
	//Time::m_Result = std::chrono::system_clock::now();
}
